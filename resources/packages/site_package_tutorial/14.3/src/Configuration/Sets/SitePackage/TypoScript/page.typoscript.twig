page = PAGE
page {
    10 = PAGEVIEW
    10 {
        paths {
            0 = EXT:{{ package.extensionKey }}/Resources/Private/Templates/
            10 = {${{ package.packageName }}.template_path}
        }
    }

    shortcutIcon = {${{ package.packageName }}.favicon}
}
